// 请求返回code
export enum ResponseCodeEnum {
  /** 成功 */
  success = 200
}

// 用户token
export const ACCESS_TOKEN_KEY = 'ACCESS_TOKEN'

// 分页
export enum Paging {
  page = 'page',
  pageSize = 'page_size'
}
